<h1>Add a new cocktail</h1>
<%= simple_form_for [@dose, @cocktail] do |f| %>
  <%= f.association :ingredient %>
  <%= f.input :description %>
  <%= f.submit "Create cocktail", class: "btn btn-primary" %>
<% end %>

<%= link_to "Back", cocktail_path(@cocktail.id) %>
